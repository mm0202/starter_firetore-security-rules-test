{
  "name": "starter_firestore-security-rules-test",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "jest --forceExit --detectOpenHandles --maxWorkers=1",
    "emu:start:firestore": "firebase emulators:start --only firestore"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@firebase/testing": "^0.15.1",
    "@types/jest": "^24.9.1",
    "jest": "^24.9.0",
    "ts-jest": "^24.3.0",
    "typescript": "latest",
    "@mm0202/firestore-client": "latest"
  }
}
